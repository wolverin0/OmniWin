<UserControl x:Class="OmniWin.UI.Views.AppMigrationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="1000"
             Foreground="{DynamicResource TextPrimary}"
             FontFamily="Segoe UI Variable, Segoe UI, sans-serif"
             Loaded="UserControl_Loaded">

    <Grid Margin="0,12,0,0">
        <Grid.RowDefinitions>
            <!-- Header Card -->
            <RowDefinition Height="Auto" />
            <!-- Migration Form & Space Diagnostics -->
            <RowDefinition Height="Auto" />
            <!-- Active Migrations List -->
            <RowDefinition Height="*" />
            <!-- Status Footer -->
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Top Header -->
        <Border Grid.Row="0" Background="{DynamicResource CardDark}" BorderBrush="{DynamicResource CardBorder}" BorderThickness="1" CornerRadius="8" Padding="18" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <Border Background="#1E1B4B" CornerRadius="4" Padding="8,3" Margin="0,0,10,0">
                            <TextBlock Text="NTFS DIRECTORY JUNCTIONS" FontSize="11" FontWeight="Bold" Foreground="#818CF8" />
                        </Border>
                        <TextBlock Text="Migrador de Juegos y Aplicaciones entre Discos" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" />
                    </StackPanel>
                    <TextBlock Text="Mueve carpetas pesadas (Steam, Epic, Adobe) a otra unidad y crea enlaces Junction transparentes: tu software sigue funcionando igual sin reinstalar." FontSize="12" Foreground="{DynamicResource TextSecondary}" />
                </StackPanel>

                <Button Grid.Column="1" Content="🔍 Escanear Candidatos en C:\" Click="BtnScanCandidates_Click" Style="{DynamicResource SecondaryButton}" Padding="12,6" FontSize="12" VerticalAlignment="Center" />
            </Grid>
        </Border>

        <!-- Migration Form & Space Diagnostics -->
        <Border Grid.Row="1" Background="{DynamicResource CardDark}" BorderBrush="{DynamicResource CardBorder}" BorderThickness="1" CornerRadius="8" Padding="18" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="CONFIGURAR MIGRACIÓN" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,12" />

                <!-- Source Path -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="130" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Carpeta Origen:" FontSize="12" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center" />
                    <TextBox Grid.Column="1" x:Name="TxtSourcePath" Text="" Background="{DynamicResource BgInput}" Foreground="{DynamicResource TextPrimary}" BorderBrush="{DynamicResource CardBorder}" Padding="8,6" FontSize="12" VerticalAlignment="Center" TextChanged="TxtSourcePath_TextChanged" />
                    <Button Grid.Column="2" Content="📁 Examinar..." Click="BtnBrowseSource_Click" Style="{DynamicResource SecondaryButton}" Margin="8,0,0,0" Padding="12,6" FontSize="12" VerticalAlignment="Center" />
                </Grid>

                <!-- Target Root Directory -->
                <Grid Margin="0,0,0,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="130" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Carpeta Destino:" FontSize="12" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center" />
                    <TextBox Grid.Column="1" x:Name="TxtTargetRoot" Text="D:\Games" Background="{DynamicResource BgInput}" Foreground="{DynamicResource TextPrimary}" BorderBrush="{DynamicResource CardBorder}" Padding="8,6" FontSize="12" VerticalAlignment="Center" TextChanged="TxtTargetRoot_TextChanged" />
                    <Button Grid.Column="2" Content="📁 Examinar..." Click="BtnBrowseTarget_Click" Style="{DynamicResource SecondaryButton}" Margin="8,0,0,0" Padding="12,6" FontSize="12" VerticalAlignment="Center" />
                </Grid>

                <!-- Space Diagnostics Banner -->
                <Border Background="{DynamicResource CardDarkHover}" CornerRadius="6" Padding="14" Margin="0,0,0,14" BorderBrush="{DynamicResource CardBorder}" BorderThickness="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel VerticalAlignment="Center">
                            <TextBlock x:Name="TxtDiagnosticsTitle" Text="Selecciona una carpeta para analizar el tamaño y espacio disponible." FontSize="12.5" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}" />
                            <TextBlock x:Name="TxtDiagnosticsSubtitle" Text="Verificaremos que el disco de destino tenga suficiente espacio libre." FontSize="11.5" Foreground="{DynamicResource TextSecondary}" Margin="0,3,0,0" />
                        </StackPanel>

                        <Border Grid.Column="1" x:Name="BadgeSpaceStatus" Visibility="Collapsed" Background="#064E3B" CornerRadius="4" Padding="10,4" Margin="0,0,14,0" VerticalAlignment="Center">
                            <TextBlock x:Name="TxtSpaceBadge" Text="ESPACIO SUFICIENTE" FontSize="11" FontWeight="Bold" Foreground="#34D399" />
                        </Border>

                        <Button Grid.Column="2" x:Name="BtnExecuteMigration" Content="🚀 Iniciar Migración &amp; Junction" Click="BtnExecuteMigration_Click" Style="{DynamicResource PrimaryButton}" Padding="16,8" FontSize="12.5" FontWeight="Bold" VerticalAlignment="Center" IsEnabled="False" />
                    </Grid>
                </Border>

                <!-- Progress Bar -->
                <StackPanel x:Name="PnlProgress" Visibility="Collapsed">
                    <ProgressBar x:Name="PbMigration" Height="6" Maximum="100" Margin="0,0,0,6" Foreground="#3B82F6" Background="{DynamicResource CardDarkHover}" />
                    <TextBlock x:Name="TxtProgressDetails" Text="Copiando archivos..." FontSize="11.5" Foreground="{DynamicResource TextSecondary}" />
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Active Migrations History DataGrid -->
        <Border Grid.Row="2" Background="{DynamicResource CardDark}" BorderBrush="{DynamicResource CardBorder}" BorderThickness="1" CornerRadius="8" Padding="18">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="MIGRACIONES Y ENLACES JUNCTION ACTIVOS" FontSize="12" FontWeight="Bold" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,12" />

                <DataGrid Grid.Row="1" x:Name="DgMigrations" AutoGenerateColumns="False" IsReadOnly="True"
                          Background="Transparent" BorderThickness="0" GridLinesVisibility="None" HeadersVisibility="Column"
                          Foreground="{DynamicResource TextPrimary}" RowBackground="Transparent">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Ubicación Original (Junction)" Binding="{Binding SourcePath}" Width="2*" />
                        <DataGridTextColumn Header="Destino Físico Real" Binding="{Binding TargetPath}" Width="2*" />
                        <DataGridTextColumn Header="Tamaño" Binding="{Binding TotalBytesText}" Width="110" />
                        <DataGridTextColumn Header="Fecha Migración" Binding="{Binding DateText}" Width="120" />
                        <DataGridTemplateColumn Header="Acción" Width="140">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="↩️ Revertir" Click="BtnRollbackRow_Click" Tag="{Binding SourcePath}" Style="{DynamicResource SecondaryButton}" Padding="10,4" FontSize="11" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Status Footer -->
        <TextBlock Grid.Row="3" x:Name="TxtStatus" Text="Listo para migrar aplicaciones y liberar espacio en disco." FontSize="11.5" Foreground="{DynamicResource TextSecondary}" Margin="2,8,0,0" />
    </Grid>
</UserControl>
