<UserControl x:Class="OmniWin.UI.Views.UsbDoctorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="1000"
             Foreground="{DynamicResource TextPrimary}"
             FontFamily="Segoe UI Variable, Segoe UI, sans-serif"
             Loaded="UserControl_Loaded">

    <Grid Margin="0,12,0,0">
        <Grid.RowDefinitions>
            <!-- Hero Header -->
            <RowDefinition Height="Auto" />
            <!-- Main Content Area (Drive List & Diagnostics) -->
            <RowDefinition Height="*" />
            <!-- Status Footer -->
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Top Header Card -->
        <Border Grid.Row="0" Background="{DynamicResource CardDark}" BorderBrush="{DynamicResource CardBorder}" BorderThickness="1" CornerRadius="8" Padding="18" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <Border Background="#1E1B4B" CornerRadius="4" Padding="8,3" Margin="0,0,10,0">
                            <TextBlock x:Name="TxtDrivesBadge" Text="0 UNIDADES" FontSize="11" FontWeight="Bold" Foreground="#818CF8" />
                        </Border>
                        <TextBlock Text="USB &amp; Storage Doctor (Expulsor Seguro &amp; Anti-Falsificación)" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" />
                    </StackPanel>
                    <TextBlock Text="Desbloquea y expulsa pendrives atascados en 'dispositivo en uso', detecta memorias flash falsas/trucadas y purga espacio libre." FontSize="12" Foreground="{DynamicResource TextSecondary}" />
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="BtnRefreshDrives" Content="🔄 Actualizar" Click="BtnRefreshDrives_Click" Style="{DynamicResource SecondaryButton}" Margin="0,0,8,0" Padding="12,6" FontSize="12" />
                    <Button x:Name="BtnEjectSelected" Content="⏏️ Expulsar Seguro" Click="BtnEjectSelected_Click" Background="#2563EB" Foreground="White" Margin="0,0,8,0" Padding="14,6" FontSize="12" />
                    <Button x:Name="BtnForceEject" Content="⚡ Forzar Cierre &amp; Expulsar" Click="BtnForceEject_Click" Background="#DC2626" Foreground="White" Padding="14,6" FontSize="12" ToolTip="Cierra procesos bloqueadores y desmonta el volumen" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Middle Content Split: Left (Drives List), Right (Doctor Tools) -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="420" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Left: Removable Drives List -->
            <Border Grid.Column="0" Background="{DynamicResource CardDark}" BorderBrush="{DynamicResource CardBorder}" BorderThickness="1" CornerRadius="8" Padding="14">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="DISPOSITIVOS EXTRAÍBLES DETECTADOS" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,10" />

                    <ListBox Grid.Row="1" x:Name="LbDrives" Background="Transparent" BorderThickness="0" SelectionChanged="LbDrives_SelectionChanged">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#090D18" BorderBrush="{DynamicResource CardBorder}" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,0,0,8">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="💾" FontSize="24" VerticalAlignment="Center" Margin="0,0,12,0" />
                                        <StackPanel Grid.Column="1">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding DriveLetter}" FontWeight="Bold" FontSize="14" Foreground="{DynamicResource TextPrimary}" />
                                                <TextBlock Text="{Binding VolumeLabel, StringFormat=' ({0})'}" FontWeight="SemiBold" FontSize="13" Foreground="#38BDF8" Margin="4,0,0,0" />
                                            </StackPanel>
                                            <TextBlock Text="{Binding FileSystem, StringFormat='Sistema de archivos: {0}'}" FontSize="11" Foreground="{DynamicResource TextSecondary}" Margin="0,2,0,4" />
                                            <ProgressBar Value="{Binding UsedPercent}" Maximum="100" Height="5" Foreground="#10B981" Margin="0,0,0,4" />
                                            <TextBlock Text="{Binding FreeSizeGB, StringFormat='Libre: {0:N1} GB disponibles'}" FontSize="10.5" Foreground="{DynamicResource TextSecondary}" />
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <!-- Right: Diagnostics & Actions for Selected Drive -->
            <Border Grid.Column="2" Background="{DynamicResource CardDark}" BorderBrush="{DynamicResource CardBorder}" BorderThickness="1" CornerRadius="8" Padding="18">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- Lock Inspector Card -->
                        <Border Background="#090D18" BorderBrush="{DynamicResource CardBorder}" BorderThickness="1" CornerRadius="6" Padding="14" Margin="0,0,0,14">
                            <StackPanel>
                                <TextBlock Text="PROCESOS BLOQUEADORES (¿POR QUÉ NO SE PUEDE EXPULSAR?)" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,8" />
                                <TextBlock x:Name="TxtLocksSummary" Text="Selecciona una unidad para analizar procesos bloqueadores." FontSize="12.5" Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,8" />

                                <ItemsControl x:Name="IcLocks">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#1E1B4B" BorderBrush="#312E81" BorderThickness="1" CornerRadius="4" Padding="8,4" Margin="0,0,0,6">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="🔒 PID " FontSize="11.5" Foreground="#F87171" FontWeight="Bold" />
                                                        <TextBlock Text="{Binding ProcessId}" FontSize="11.5" Foreground="#F87171" FontWeight="Bold" Margin="0,0,8,0" />
                                                        <TextBlock Text="{Binding ProcessName}" FontSize="11.5" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}" Margin="0,0,8,0" />
                                                        <TextBlock Text="{Binding ApplicationType, StringFormat='({0})'}" FontSize="11" Foreground="{DynamicResource TextSecondary}" />
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>

                        <!-- Fake Flash Tester Card -->
                        <Border Background="#090D18" BorderBrush="{DynamicResource CardBorder}" BorderThickness="1" CornerRadius="6" Padding="14" Margin="0,0,0,14">
                            <StackPanel>
                                <TextBlock Text="DETECTOR DE CHIPS FLASH FALSOS / TRUCADOS (ANTI-FRAUDE)" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,6" />
                                <TextBlock Text="Escribe bloques de datos de prueba y los verifica byte a byte para descubrir si un pendrive comprado tiene menor capacidad física de la que reporta." FontSize="11.5" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,10" TextWrapping="Wrap" />

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                    <TextBlock Text="Tamaño de test:" VerticalAlignment="Center" FontSize="11.5" Foreground="{DynamicResource TextPrimary}" Margin="0,0,8,0" />
                                    <ComboBox x:Name="CbTestSize" Width="120" SelectedIndex="0" Margin="0,0,12,0" Background="#050811">
                                        <ComboBoxItem Content="1 GB (Rápido)" Tag="1" />
                                        <ComboBoxItem Content="4 GB (Estándar)" Tag="4" />
                                        <ComboBoxItem Content="8 GB (Profundo)" Tag="8" />
                                    </ComboBox>
                                    <Button x:Name="BtnRunFlashTest" Content="🧪 Iniciar Test de Integridad" Click="BtnRunFlashTest_Click" Background="#D97706" Foreground="White" Padding="12,6" FontSize="11.5" />
                                </StackPanel>

                                <ProgressBar x:Name="PbFlashTest" Value="0" Maximum="100" Height="6" Margin="0,0,0,6" Foreground="#F59E0B" Visibility="Collapsed" />
                                <TextBlock x:Name="TxtFlashResult" Text="Listo para probar celdas de almacenamiento." FontSize="11.5" Foreground="{DynamicResource TextSecondary}" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>

                        <!-- Free Space Wipe Card -->
                        <Border Background="#090D18" BorderBrush="{DynamicResource CardBorder}" BorderThickness="1" CornerRadius="6" Padding="14">
                            <StackPanel>
                                <TextBlock Text="BORRADOR SEGURO DE ESPACIO LIBRE (ZERO-FILL)" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,6" />
                                <TextBlock Text="Sobrescribe todo el espacio disponible con ceros para asegurar que archivos eliminados previamente no puedan recuperarse con herramientas forenses." FontSize="11.5" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,10" TextWrapping="Wrap" />
                                <Button x:Name="BtnWipeFreeSpace" Content="🧹 Purgar Espacio Libre con Ceros" Click="BtnWipeFreeSpace_Click" Style="{DynamicResource SecondaryButton}" Padding="12,6" FontSize="11.5" HorizontalAlignment="Left" />
                                <TextBlock x:Name="TxtWipeStatus" Text="No hay proceso de purga en ejecución." FontSize="11" Foreground="{DynamicResource TextSecondary}" Margin="0,6,0,0" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Status Footer -->
        <Border Grid.Row="2" Background="#090D18" BorderBrush="{DynamicResource CardBorder}" BorderThickness="1" CornerRadius="6" Padding="12" Margin="0,12,0,0">
            <TextBlock x:Name="TxtFooterStatus" Text="Conecta o selecciona una unidad USB para iniciar diagnósticos." FontSize="12" Foreground="{DynamicResource TextSecondary}" />
        </Border>
    </Grid>
</UserControl>
